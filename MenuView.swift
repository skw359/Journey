import SwiftUI

struct MenuView: View {

    var body: some View {
        VStack {
            Button("Option 1") {
                // Action for Option 1
            }
            Button("Option 2") {
                // Action for Option 2
            }
            // Add more buttons as needed
        }
    }
}
